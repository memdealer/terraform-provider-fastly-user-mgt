# Terraform state and cache
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Exclude CLI config (may contain credentials)
*.tfrc

# Exclude any tfvars files with secrets
*.tfvars
*.tfvars.json
!example.tfvars

